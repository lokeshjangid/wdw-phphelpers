A collection of php helper functions. Aimed at using it across many projects, almost all projects. So i've created it in the form of a very simple wordpress plugin. So all those functions are just a click away. 